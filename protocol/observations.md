# IR Observations

The following are the observed IR codes for the Voltas AC unit. These codes were captured using an IR receiver and analyzed to determine the corresponding commands.

```yaml
remote_receiver:
  pin: GPIO5
  dump: all
  id: ir_receiver
```
